import XCTest
@testable import Checkout_Flow_Feature

final class Checkout_Flow_FeatureTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Checkout_Flow_Feature().text, "Hello, World!")
    }
}
